import React from 'react';
import { SafeAreaView, View } from 'react-native';

const LoginScreen = () => {
  return (
    <SafeAreaView>
      <View />
    </SafeAreaView>
  );
};

export default LoginScreen;
